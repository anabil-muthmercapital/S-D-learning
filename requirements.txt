# Pinned for both local dev (anaconda 3.13) and GitHub Actions (3.12).
# Versions match what was used to TRAIN xgb_model.json — changing major
# versions can break model load or produce numerically-different features.
pandas==2.2.3
numpy==2.1.3
xgboost==3.1.2
yfinance==1.0
scikit-learn==1.6.1
scipy==1.15.3
pydantic==2.12.4
joblib==1.4.2
tqdm==4.67.1
